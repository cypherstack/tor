name: socks_socket
description: SOCKS5 client implementation for Dart and Flutter.  Now with SSL!
version: 0.1.0
repository: https://github.com/cypherstack/socks_socket

environment:
  sdk: ^3.0.6

dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
